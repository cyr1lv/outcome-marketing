import { heading, paragraph, textBlock } from "../styles";

export function NativeOutcome() {
  return (
    <section style={textBlock}>
      <h2 style={heading}>Native Outcome Infrastructure</h2>
      <p style={paragraph}>
        It replaces existing software with an AI-native architecture. Mandate, the Max
        Outcome Decision Engine, and the Value Surface form the core. Migration and
        implementation are delivered by certified partners.
      </p>
    </section>
  );
}
